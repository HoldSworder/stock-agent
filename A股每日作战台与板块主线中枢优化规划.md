# A股每日作战台与板块主线中枢优化规划（修订版）

> 本版基于对现有代码的评审 + 交叉复核收敛而来，核心调整：**从「新建板块中枢」改为「收口既有能力 + 补两块真实短板」**。
> 修订依据见文末《与初版的差异》。凡涉及现有实现处均标注文件位置，避免重复造轮子。

## 摘要

将项目从"选股 / 选板块 / 模拟交易工具集合"重新定位为 **以板块主线为核心的 A股每日决策助手**。

优化主线不是继续堆功能，而是**收口已存在但分散的能力**：系统已具备驾驶舱一屏概览（`CockpitView` / `CockpitOverview`）、主线共识聚合（`buildMainlineConsensus`，以板块新高宽度为确定性锚）、结构化主线沉淀（`market_themes`）、中线雷达与强弱表。本轮重塑用户每日决策流程——先判断市场环境，再识别主线板块，再下钻 ETF / 龙头 / 补涨，最后联动持仓、自选、盯盘、AI 研判与盘后复盘——并解决两块真实短板：**跨源板块的稳定关联键**与**持仓/自选的板块暴露反查**。

## 产品重构

- **改造首页驾驶舱（不是新建）**：首页 `/` 已是 `CockpitView`，后端已有 `CockpitOverview` 一屏模型（`safety` / `plan` / `planStance` / `themes` / `modules` / `screenerPicks` / `events`）。注意"当日盈亏归因"**不在 `CockpitOverview` 返回体内**，是 `CockpitView` 额外旁路拉取 `api.attribution()` 的只读数据。本轮把呈现**从"模块摘要卡"改造为"每日决策流"**，复用既有数据，不新起页面。
- **功能按交易日节奏组织**：
  - 盘前：市场环境、重点板块、备选 ETF / 个股、今日计划。
  - 盘中：主线验证、异动提醒、持仓风险、禁止追高 / 退潮提示。
  - 盘后：复盘归因、主线延续 / 证伪、明日观察池。
- **所有 AI 结论固定为行动结构**：结论、理由、证据、失效条件、建议动作，减少长篇报告式输出。
- **每个关键页面都回答一个问题**：现在该观察、试错、持有、减仓、回避，还是等待。

## 板块主线中枢

- **真相源 = 确定性结构，`market_themes` = 结构化证据/沉淀层**（关键修订）：
  - 决策锚沿用现有 `buildMainlineConsensus` 的设计——以 **breadth 板块新高宽度**（`verdict: confirmed/candidate`，与今日计划 `focusSectors` 同口径）为确定性硬证据，叠加 RS / 中线趋势。
  - `market_themes` 定位为**多源结构化沉淀层**（涨幅榜 / 主力资金 / 复盘 / 热点 / 研报归并沉淀），作为强度与阶段的证据 overlay，**不作为唯一决策真相源**，避免主线判断被滞后的涨幅/热度污染而追高。
- **板块列表从榜单改为决策卡片**：每个板块展示阶段、适合周期、操作标签、强度趋势、核心证据、风险信号、关联 ETF / 龙头 / 补涨。
- **板块阶段先稳住现有 5 态，扩展需先补量化触发条件**（关键修订）：
  - 现有 `ThemePhase` = `启动 / 加速 / 分歧 / 退潮 / 未知`，由复盘 verdict 回流写入。
  - 如需扩展"高潮 / 修复"，**必须先定义每态的量化触发规则与数据来源**（示例：高潮 = 连板高度见顶 + 换手骤升 + 溢价率拐头；修复 = 分歧后主力资金回流 + 新高宽度重新走阔），否则不扩。
  - **禁止新增"观察"作为生命周期阶段**——"观察"已是 `MainlineConsensusLevel`（共识档 resonance/diverge/watch），再复用会造成语义冲突。
- **周期视角是"展示/建议维度"，与 5 态生命周期正交，不得混成一个状态机**（SHOULD）：`ThemePhase`（启动/加速/分歧/退潮）是板块**生命周期状态**；下列超短/短线/波段/中线/长线是**交易周期视角**，二者是两个维度（同一"加速"阶段板块，对短线与中线的操作建议不同），实现上分开建模。
  - 超短 / 短线：1-5 日，看情绪、涨停、资金、催化。
  - 波段：5-20 日，看轮动、扩散、补涨、分歧修复。
  - 中线主线：20-60 日，看 RS、宽度、趋势、ETF。
  - 长线配置：60 日以上，看估值、产业、政策、盈利周期。
- **板块详情形成闭环**：趋势与盘口、主线证据、板块内标的、ETF 替代、持仓暴露、短中长线建议、失效条件。

## 一致性地基：稳定板块关联键（关键修订）

- **问题**：当前跨源关联（breadth ⋈ themes ⋈ radar）靠 `buildMainlineConsensus` 里的 `nameMatch` 模糊包含匹配（如"机器人"⋈"工业机器人"），既会漏配也会错配；且 `market_themes.boardCode` 可空。这是"同一板块各页阶段/强度不冲突"验收标准的**真实阻断项**。
- **对策**：
  - 建立**稳定 join key**：以东财板块代码 `boardCode` 为主键，回填 `market_themes.boardCode`。
  - `nameMatch` 降级为仅在无 boardCode 时的兜底，并记录未命中项供人工校准。
  - 所有页面（首页、大盘、持仓、自选、AI）的板块阶段/强度**统一取自同一次聚合产物**，不各自计算。
- **`boardCode` 回填实施规则**（不能只写"对齐 breadth/radar"）：
  - **数据源分层**：breadth / radar 天然带东财板块 code，可精确回填；但 `market_themes` 里由**计划 / 复盘创建的主题常无 code**，需按「精确 code → 板块名搜索（`searchBoard`）→ 人工校准表」三级回填。
  - **冲突处理**：一个主题名搜到多个候选 code 时不自动写入，进人工校准队列。
  - **空值保留**：搜不到的保留 `boardCode = null`（不猜），继续走 `nameMatch` 兜底，并留痕未命中清单。
  - **人工校准表**：维护 `theme → boardCode` 覆盖映射，优先级最高。
  - **验收**：回填脚本产出「精确命中 / 名称命中 / 待校准 / 空值」四类计数报表；跑一次全量后待校准项可人工清零。

## 关键接口与类型

- **`BoardWorkbenchItem` / `BoardWorkbenchDetail` 以 `MainlineConsensusItem` 为主干，另接 resolver**（关键修订）：
  - **主干（视图投影）**：阶段、强度、强度趋势、ETF、共识档等**直接投影自 `MainlineConsensusItem`**（现有字段仅 board / etf / breadth / theme / radar / note），不新起板块判断数据源，避免形成"第四套板块表示"。
  - **另接 resolver（投影覆盖不到的）**：龙头、补涨、关联成分标的、风险标签、行动标签、失效条件，`MainlineConsensusItem` 里**没有这些数据**，需由独立 resolver 计算/取数后挂接（见下方"标的解析"与"龙头/补涨定义"）。不能声称"纯视图投影即可覆盖"。
- **扩展 `MarketTheme` 展示能力**：增加操盘阶段、周期适配、行动标签、风险标签、关联标的、失效条件等派生字段（派生自共识产物，不改沉淀语义）。
- **新增接口建议**：
  - `GET /api/boards/workbench`：返回板块工作台列表（共识产物投影）。
  - `GET /api/boards/:code/detail`：返回单板块详情（以 boardCode 为键）。
  - `GET /api/boards/exposure`：返回持仓 / 自选 / 策略标的的板块暴露（见下方专项）。
- **前端落点：大盘页 board Tab 内升级，不新增一级导航、不承诺升主导航**（关键修订）：
  - 现有 IA 处于强收敛期（`/themes`、`/radar` 已重定向进 `/market`，情报/账户已二合一），大盘页 board Tab 已承接主线共识 + 热门概念 + breadth/themes/strength 折叠明细。
  - 板块主线工作台**就在 board Tab 内升级**，删除初版"稳定后提升主导航"的表述。

## 专项：持仓 / 自选板块暴露反查（`/api/boards/exposure`）

- **价值**：这是本轮**真正的新增能力**——反查用户持仓/自选是否处于主线、退潮或拥挤状态，散户最缺。
- **工作量重估（非从零）**：仓库已有"板块→成分股"取数能力（breadth 经东财取板块成分、concepts 经问财展开成分、rotation ETF 下钻取成分）。**真正缺的是持久化、可缓存、可复用的"个股→多板块"反向索引**（持仓表当前只有 code/name 等仓位字段，`computeSectorNote` 仅临时取"所属行业→searchBoard"，非索引）。
- **前置 POC（先做再承诺工期）**：
  - 验证"个股 → 所属行业(1) + 所属概念(N)"成分数据的**覆盖率、时延、缓存刷新频率**。
  - 定义一只票命中多个概念时的"主线暴露"归属规则（建议：优先命中当前 confirmed/candidate 主线板块）。
- **反向索引存储与刷新设计**（MUST，不能一笔带过）：
  - **新增表** `stock_board_index`：`stock_code → board_code[]`（含 `source`、`updatedAt`、`snapshotDate`）。
  - **来源与优先级**：行业成分（东财，稳定）> 概念成分（问财，`concepts/data.ts` 5 分钟缓存、按需）；ETF 成分（`etf/data.ts` 6 小时缓存）仅用于 ETF 维度暴露。
  - **刷新触发与时点**：随 breadth 收盘快照（30 分钟响应缓存）与概念成分刷新**增量更新**；概念侧按需拉取的部分需落快照，避免每次 exposure 查询实时打问财。
  - **一致性**：exposure 结果标注 `snapshotDate`，与 breadth 快照时点对齐，避免"持仓风险"用到过期成分。

## 专项：标的解析（龙头 / 补涨 / 关联成分）

- **背景**：`MainlineConsensusItem` 不含成分/龙头/补涨，需独立 resolver 挂接到 `BoardWorkbenchDetail`。
- **龙头（最小可执行定义）**：板块成分内按「市值 + 个股强度/RS + 趋势状态」排序取头部，先用确定性排序，不依赖 AI 文案。
- **补涨（最小可执行定义）**：需同时满足「相对主线强度偏低（尚未大涨）+ 位置不高 + 近日资金确认」三条，否则不标补涂，避免退化为 AI 主观措辞。
- **数据复用**：成分取数复用 `concepts/data.ts`（概念）/ breadth 板块成分 / `rotation/service.ts`（ETF 下钻），resolver 只做筛选排序，不新起取数源。

## 实施优先级

> **P0 拆分说明**：稳定关联键是驾驶舱一致性的前置依赖，必须先行；驾驶舱改造只消费其稳定产物，不得先行承诺"phase/strength 完全相等"。exposure 因 P0 页面要显示"持仓板块风险"，其 POC 探针需前置到 P0，否则 P0 页面会先做出半成品。

### P0a：稳定关联键 + 聚合产物单一来源（前置）

- 明确真相源分层：确定性结构为锚，`market_themes` 为证据/沉淀层。
- 回填 `boardCode` 稳定 join key，替换 `nameMatch` 为兜底，统一各页取同一聚合产物。
- 建立板块决策卡片和阶段标签（沿用 5 态；扩展需带量化触发条件）。
- **exposure POC 探针**：先验证成分覆盖率/时延，供 P0b 的"持仓板块风险"托底。
- 粗工作量：中。验收：一致性判据（见验收标准）可通过。

### P0b：改造驾驶舱为每日决策流（消费 P0a 产物）

- 首页 `CockpitView` 从模块摘要改为盘前/盘中/盘后决策流（复用 `CockpitOverview` + 旁路归因）。
- 输出市场状态、主线板块、持仓风险、今日动作，**均取自 P0a 稳定聚合产物**。
- 动作标签统一为：观察、试错、持有、加仓候选、减仓、回避、等待。
- 粗工作量：中。验收：首页 30 秒回答四问；持仓风险取值与 P0a 一致。

### P1：板块到标的闭环

- 每个板块产出三类可执行标的：ETF、龙头、补涨（按"标的解析"最小定义实现）。
- **持仓/自选板块暴露反查**（落地反向索引表，见专项）。
- 选股器支持从板块下钻触发。
- 粗工作量：大（含 exposure 反向索引 + 标的 resolver）。验收：任一主线可下钻三类标的并反查持仓。

### P1：盘前 / 盘中 / 盘后流程

- 盘前生成今日计划。
- 盘中验证主线和风险。
- 盘后复盘昨日判断是否延续、证伪或退潮。

### P2：AI 与术语降噪（需先与在建模块对齐）

- 收敛重复 AI 入口。
- 所有专业指标提供白话解释。
- 回测、因子、策略模拟默认后置为高级能力——**执行前先与正在建设的 factor / modes / sectorintel 模块负责人对齐边界**，避免"一边收敛一边扩张"。
- **注意**："所有 AI 结论固定为行动结构（结论/理由/证据/失效条件/动作）"会改动 prompt 与输出 schema，**该输出契约应在 P1 同步定义并落地**（不能等 P2），否则 P0b/P1 的 AI 卡片会先用不一致的结构。

## 验收标准

- 用户打开首页 30 秒内能回答：今天能不能做、主线是什么、我手里的票有没有风险、下一步动作是什么。
- 板块页不再只是涨跌榜，而能明确区分短线热点、波段轮动、中线主线和长线赛道。
- 同一板块在首页、大盘页、持仓页、自选页、AI 研判中的阶段和强度不冲突。
- **可执行一致性判据（新增）**：任取一个板块，断言它在 cockpit / market / positions 三处的 `phase` 与 `strength` **来自同一次聚合产物、值完全相等**（以 boardCode 为键校验）。
- 任意主线板块都能下钻到 ETF、龙头、补涨，并能反查用户是否已有相关持仓或自选。
- AI 输出不再要求用户自己读报告总结，而是直接给结论、证据、失效条件和动作建议。

## 假设

- 当前系统主要服务辅助决策与模拟验证；本轮**沿用现有安全总闸 / 急停 / 自动模拟开关机制（`SafetyState`），不回退、不将实盘下单风控作为本轮重点**。
- 默认核心打法是板块主线驱动：中线主线与 ETF 轮动为主轴，短线用于情绪验证，长线用于配置参考。决策锚为确定性结构，情绪/热度类信号仅作证据。
- 第一阶段优先重组信息架构与决策链路，不新增过多外部数据源；`exposure` 复用既有成分取数能力，仅补反向索引。
- 本轮在剧烈变更的代码基线上进行，需先厘清在建的 factor / modes / sectorintel / verdict 等未合入模块与板块中枢的边界。

---

## 与初版的差异（评审收敛结果）

| # | 初版表述 | 修订后 | 原因 |
|---|---|---|---|
| 1 | P0 新建"今日作战台" | 改造现有 `CockpitView` / `CockpitOverview` | 首页驾驶舱与一屏模型已存在 |
| 2 | 唯一真相源 = `market_themes` | 确定性结构为锚，`market_themes` 为结构化证据/沉淀层 | 现有 `buildMainlineConsensus` 已以新高宽度为确定性锚；软证据作锚易追高 |
| 3 | 阶段扩到 7 态（+高潮/修复/观察） | 沿用 5 态；扩展先补量化触发；"观察"禁止复用 | 新态无数据来源；"观察"与 `MainlineConsensusLevel` 撞名 |
| 4 | 未提跨源关联 | 新增"稳定 boardCode 关联键"章节 | `nameMatch` 模糊匹配 + `boardCode` 可空是一致性真实阻断 |
| 5 | 新增 `BoardWorkbench` DTO | 做成 `buildMainlineConsensus` 的视图投影 | 避免第四套板块表示 |
| 6 | `exposure` 一个接口带过 | 拆为专项 + 前置 POC；明确缺"个股→板块反向索引" | 已有成分取数能力，缺的是反向索引，工作量被低估 |
| 7 | 新增一级导航，稳定后升主导航 | 落大盘 board Tab 内升级，删除"升主导航" | 逆现有 IA 强收敛趋势 |

### 终审补充修正（第三轮，已落地本版）

- **归因不在 `CockpitOverview` 内**：改为"旁路 `api.attribution()`"，避免误导实现去改聚合返回体。
- **`BoardWorkbench` 非纯视图投影**：阶段/强度/ETF 走投影，龙头/补涨/风险/失效条件须另接 resolver（新增《标的解析》专项 + 龙头/补涨最小定义）。
- **`boardCode` 回填补实施规则**：三级回填、冲突入校准队列、空值保留、四类计数验收。
- **exposure 补存储/刷新设计**：新增 `stock_board_index` 表、来源优先级、快照时点与各缓存的关系。
- **P0 拆为 P0a（稳定关联键，前置）/ P0b（驾驶舱消费产物）**：一致性验收不得先于关联键；exposure POC 探针前置到 P0。
- **周期视角与 5 态生命周期正交**，不混成状态机；AI 行动结构输出契约前置到 P1 定义。
- 各优先级补了粗工作量与验收样例。

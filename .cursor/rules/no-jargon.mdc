---
description: 界面文案禁用内部黑话，术语必须写成散户看得懂的话
alwaysApply: true
---

# 界面文案禁黑话

本项目只有一个使用者：做 A 股 ETF 中线主升浪的散户，无量化背景。凡是他看得到的字，都必须是他不查资料就能懂的。

判定标准：**这句话要读者具备本系统内部知识、软件工程背景或量化背景才能理解吗？** 是，就得改。

## 禁用词与替代

| 禁用 | 为什么 | 改成 |
| --- | --- | --- |
| 确定性 | 会被读成"确定会涨"，语义相反。实际指"按规则算的、不经 LLM" | 按句改写成「按规则计算的结果」，不要机械换词 |
| 断言 | 自造词，像编程 `assert` | 技术判断记录 |
| 冻结 | 会被读成"资金冻结"。仓库里存档义和禁止义混用 | 存档义→存档；禁止建仓义→禁止建仓 |
| 口径 | 统计工程词 | 按语境写成 计算方式 / 数据依据 / 时间范围 |
| 下钻 | BI 术语 | 赛道选股义→挑出；打开详情义→展开明细 |
| 降级 | 工程词，会被读成"服务缩水" | 数据不全 / 降为观望 |
| 暴露 | 会被读成"隐私暴露" | 持仓/自选关联板块 |
| 读模型 / 收口 / 打点 / 治理 / 限流 / 反哺 / 底稿 / 前向验证 | 纯工程词泄漏 | 汇总 / 调用日志 / 清理 / 超出本轮上限 / 不会自动改参数 / 原始数据 / 用之后的新数据检验 |
| 晋级门 / 协议号 / 有效簇数 | 自造统计词 | 达标检验 / 规则版本号 / 独立样本数 |
| superseded | 英文直出 | 已被后来的判断推翻 |

## 允许保留

A 股通用语不用改，使用者本来就用这套词交易：金叉/死叉、背离、量比、多头排列、连板/首板/炸板、龙虎榜、游资、北向、前复权、筹码、折溢价、主升/退潮、研判、涨停溢价。

`FactorView` 的量化词（IC/ICIR/z-score/横截面）保留，前提是该页继续明示为高级实验入口。

## 同词异义要消歧

同一个词在不同页面指不同东西，比生僻词更容易误解。写文案前先确认这一处是哪个意思：

- **中枢**：导航栏=控制台；K 线图=缠论中枢
- **档位**：K 线=价位；计划/持仓页=仓位等级；智能体页=模型规格
- **等权**：模式库=每笔等额不滚利；大盘页=全A等权指数
- **回撤**：绩效=最大回撤；波浪=回踩幅度
- **命中**：触发价触及 / 失效条件成立 / 盯盘规则触发 / 缓存命中 / 关键词匹配

## 原值与显示名的边界

`shared/src/index.ts` 里的中文类型字面量（如 `EtfWatchVerdict = '撤层' | ...`）同时是 DB 存值和业务判断依据，**不要改字面量**。改法是加显示名映射：

```ts
// shared/src/index.ts —— 复用 SCREEN_FACTOR_LABELS / SYMBOL_PLAN_OUTCOMES 的形态
export const ETF_WATCH_VERDICT_LABELS: Record<EtfWatchVerdict, string> = {
  撤层: '减一层仓',
  // ...穷尽所有成员
};
```

- **原值**用于：DB、API、条件判断、机器日志、LLM JSON 契约
- **显示名**用于：浏览器、Telegram 推送、复制文本
- 映射必须穷尽 `Record<枚举, string>`，**禁止 `LABELS[v] ?? v` 兜底**——漏一个就继续泄漏黑话
- 显示名不得反向提交回 API

注意前端不是唯一出口：Telegram 文案由 `backend/src/etfwatch/dispatcher.ts` 独立生成，所以显示名表放 `shared` 而不是 `frontend`。

## 不要把实现细节印给用户

```ts
// ❌ 用户会以为系统坏了
`（命中决策缓存，dataAsOf ${hit.dataAsOf}，未失效，直接复用）`
`置信度 ${c} 低于 ${ELLIOTT_MIN_TRUSTED_CONFIDENCE}，不给目标位`
`【情报研判·最新】无持久化情报研判（情报合并定时未启用或未产出）`

// ✅
`（用的是今天已经算好的结果）`
`把握不足，不给目标价`
`暂无情报分析（这项没开启，可到设置里打开）`
```

同理禁止直出：英文字段名、常量名、`SQLite`、`bar`、`universe`、`L1/L2a`、`S1`、数学符号 `∩`、板块代码 `883994`。

## 提交前自检

```bash
pnpm glossary:check
```

它扫描前端与后端面向用户的文案，命中即列出 `文件:行号` 并以退出码 1 阻断。当前基线为零命中，所以新命中一定是你刚写的。

它先跑一遍自身判断的自检（12 项），再扫全仓。

改掉命中，或者——只有在确认该处不是界面文案时——把语境写进 `glossary.selfcheck.ts` 的 `allow`（按「文件片段 + 行内文本」匹配，不要用行号，行号会漂）。已自动豁免的：`PROMPT_ONLY` 里的提示词文件（只对黑话规则生效，实现细节泄漏照拦）、行首 `if (process.argv[1]` 守卫之后的自检块、注释、`${}` 插值。被守卫截断的文件会在报告末尾列出，别让漏扫无声发生。
